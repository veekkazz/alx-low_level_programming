#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: the second number to multiply with the first number
 * Return: The result of multiplying x integer and y integer
 */

int mul(int x, int y)

{
	return (x * y);
}
